import 'package:flutter/material.dart';

class AddRecipeTextField extends StatelessWidget {
  const AddRecipeTextField({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
